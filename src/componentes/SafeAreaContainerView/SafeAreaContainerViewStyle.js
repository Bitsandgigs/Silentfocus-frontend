import {StyleSheet} from 'react-native';

// Mics Constants
import {height} from '../../function/commonFunctions';

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingTop: height(1),
    },
});
